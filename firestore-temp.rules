rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORÁRIO - Permitir tudo para usuários autenticados (APENAS PARA DEBUG)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}